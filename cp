cp

This command copies a file to a new name or location.

It takes the file to copy as its first parameter and then the new name and/or location as its second

usage cp foo bar

or cp ./foo ./bar/foo
